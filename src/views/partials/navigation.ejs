<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="#">JOBUSS</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if (usuario.email != "admin@main.com") { %>

        <!-- AQUI VA EL URL QUE LLEVARA AL PERFIL DEL USUARIO -->
          <li class="nav-item"><%- (url && url.length > 0) ? url : "" %></li>

          <li class="nav-item">
            <a class="nav-link" href="/documentos">Documentos</a>
          </li>
          <% }else{%>

            <li class="nav-item">
              <a class="nav-link" href="/usuarios">Lista Usuarios</a>
            </li>
          <% } %>
        
        <li class="nav-item">
          <a class="nav-link" href="/cerrarSesion">Cerrar Sesion</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
